using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class aWaken : MonoBehaviour
{
    
    public Collider2D _collider {  get; private set; }
    






    /*void Start()
    {
        _anim = GetComponent<Animator>();
    }
    void Update()
    {
        Collider2D collider = Physics2D.OverlapCircle(transform.position, aWakenR, _playerLayerMask);
        if (collider != null)
        {
            _anim.SetTrigger("awenken");
            _collider = collider;
        }
        
    }
    private void OnDrawGizmos()
    {
        Gizmos.DrawWireSphere(transform.position, aWakenR);
    }*/
}
